Create a .env file add this bellow with ur own api key
then do pip install -r requirements.txt
```
    REDIS_HOST=localhost
    REDIS_PORT=6379
    REDIS_DB=0
    MISTRAL_API_KEY=
    MODEL_NAME=open-mistral-7b
    GPU_ENABLED=true
```